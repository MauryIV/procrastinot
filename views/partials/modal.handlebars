<!-- Modal markup for adding a new project -->
<div class="modal" id="projectModal">
  <div class="modal-background"></div>
  <div class="modal-content">
    <div class="modal-card">
      <header class="modal-card-head">
        <p class="modal-card-title">Add Project</p>
        <button class="delete close-modal-button" aria-label="close"></button>
      </header>
      <!-- Section for entering project details -->
      <section class="modal-card-body">
        <div id="newProjectSection">
          <p>Create a new project:</p>
          <!-- Input field for project title -->
          <div class="field">
            <label class="label">Title</label>
            <div class="control">
              <input
                class="input"
                type="text"
                id="newProjectTitle"
                placeholder="Enter project title"
              />
            </div>
          </div>
          <!-- Textarea for project description -->
          <div class="field">
            <label class="label">Description</label>
            <div class="control">
              <textarea
                class="textarea"
                id="newProjectDescription"
                placeholder="Enter project description"
              ></textarea>
            </div>
          </div>
        </div>
      </section>
      <!-- Footer with buttons to add a new project and close the modal -->
      <footer class="modal-card-foot">
        <button class="button is-success add-new-project-button">Add New Project</button>
        <button class="button is-danger close-modal-button">Close</button>
      </footer>
    </div>
  </div>
</div>

<!-- Script for handling modal function. -->
<script src="./js/modal.js"></script>
